query Feed($limit: Int!, $cursor: String) {
  feed(limit: $limit, cursor: $cursor) {
    id
    content
    deserved_count
    undeserved_count
    authorId
    guiltyId
    initial_balance
    status
    createdAt
    updatedAt
    author {
      id
      username
      name
    }
  }
}
